_:

{
  jacobkoziej = {
    programs = {
      document-viewers.enable = true;
    };

    services = {
      messaging.enable = true;
      web-browser.enable = true;
      windowManager.enable = true;
    };
  };
}
